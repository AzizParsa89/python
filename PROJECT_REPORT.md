## گزارش پروژه مسابقه تایپ

**تاریخ تهیه گزارش:** ۱۴۰۳/۰۴/۰۱

**تهیه کننده:** Jules (دستیار هوش مصنوعی)

### ۱. معرفی پروژه

این پروژه یک پلتفرم مسابقه تایپ است که با هدف فراهم آوردن امکانی برای کاربران جهت بهبود سرعت و دقت تایپ خود از طریق یک رابط کاربری جذاب و رقابتی طراحی شده است. پروژه از دو بخش اصلی تشکیل شده است:

*   **اپلیکیشن فرانت‌اند (موبایل):** با استفاده از فریم‌ورک **Flutter** توسعه داده شده است. این اپلیکیشن رابط کاربری اصلی برای تعامل کاربران با سیستم است و امکاناتی نظیر نمایش جملات برای تایپ، محاسبه سرعت و دقت، نمایش پروفایل کاربری و جدول رده‌بندی را فراهم می‌کند.
*   **API بک‌اند:** با استفاده از فریم‌ورک **Django** (مبتنی بر پایتون) توسعه داده شده است. این بخش مسئولیت مدیریت داده‌ها (کاربران، جملات، نتایج تایپ)، احراز هویت کاربران و ارائه سرویس‌های لازم به اپلیکیشن فرانت‌اند از طریق یک API RESTful را بر عهده دارد.

**ویژگی‌های کلیدی پیش‌بینی شده:**

*   ثبت‌نام و ورود کاربران
*   دریافت جملات تصادفی یا انتخابی برای تایپ
*   محاسبه آنی سرعت تایپ (WPM - Words Per Minute) و دقت
*   ذخیره‌سازی تاریخچه تلاش‌های تایپ کاربر
*   نمایش پروفایل کاربری با آمار و اطلاعات مربوط به تایپ
*   جدول رده‌بندی کاربران بر اساس امتیاز یا سرعت
*   پشتیبانی از چندزبانگی (در حال حاضر انگلیسی و فارسی در فرانت‌اند مشاهده شده است)

### ۲. مراحل نصب و راه‌اندازی

**الف) بخش فرانت‌اند (Flutter - `typing_app`)**

1.  **پیش‌نیازها:**
    *   نصب Flutter SDK (از وب‌سایت رسمی Flutter)
    *   نصب ابزارهای لازم برای پلتفرم هدف (Android Studio برای اندروید، Xcode برای iOS، و ...)
    *   اجرای `flutter doctor` و رفع مشکلات احتمالی.
2.  **دریافت وابستگی‌ها:**
    *   وارد دایرکتوری `typing_app` شوید.
    *   دستور `flutter pub get` را اجرا کنید تا تمام پکیج‌های مورد نیاز از `pubspec.yaml` دانلود شوند.
3.  **اجرای برنامه:**
    *   یک شبیه‌ساز را اجرا کنید یا یک دستگاه واقعی را متصل نمایید.
    *   دستور `flutter run` را برای اجرای اپلیکیشن اجرا کنید.

**ب) بخش بک‌اند (Django - `typing_competition_project`)**

1.  **پیش‌نیازها:**
    *   نصب Python (نسخه‌ای که با Django 5.2 سازگار باشد).
    *   توصیه می‌شود یک محیط مجازی (virtual environment) ایجاد و فعال شود:
        ```bash
        python -m venv venv
        # For Linux/macOS:
        source venv/bin/activate
        # For Windows:
        venv\Scripts\activate
        ```
2.  **نصب وابستگی‌ها:**
    *   **نکته:** فایل `requirements.txt` در پروژه بک‌اند موجود نیست. این فایل باید ایجاد شود و شامل وابستگی‌های زیر (و نسخه‌های دقیق‌تر در صورت امکان) باشد:
        *   `Django`
        *   `djangorestframework`
        *   `django-cors-headers`
    *   دستور نصب دستی (تا زمان ایجاد `requirements.txt`):
        ```bash
        pip install django djangorestframework django-cors-headers
        ```
3.  **اعمال Migrationها:**
    *   وارد دایرکتوری `typing_competition_project` شوید.
    *   دستور `python manage.py migrate` را برای ایجاد جداول پایگاه داده اجرا کنید.
4.  **ایجاد کاربر ادمین (اختیاری اما مفید):**
    *   دستور `python manage.py createsuperuser` را اجرا کرده و اطلاعات لازم را وارد کنید.
5.  **اجرای سرور توسعه:**
    *   دستور `python manage.py runserver` را اجرا کنید. سرور به طور پیش‌فرض روی `http://127.0.0.1:8000/` اجرا خواهد شد.

### ۳. توضیح ساختار و کدهای پروژه

**الف) `typing_app` (فرانت‌اند Flutter)**

*   **`lib/`**: دایرکتوری اصلی کد Dart.
    *   **`main.dart`**: نقطه ورود اپلیکیشن؛ تنظیمات اولیه و اجرای ویجت اصلی.
    *   **`l10n/`**: برای مدیریت چندزبانگی برنامه (انگلیسی و فارسی).
    *   **`models/`**: تعریف ساختار داده‌ها:
        *   `sentence.dart`: مدل برای جملات تایپ.
        *   `typing_attempt.dart`: مدل برای ذخیره نتایج هر تلاش تایپ.
        *   `user.dart`: مدل برای اطلاعات کاربر.
    *   **`providers/`**: مدیریت وضعیت با پکیج `provider`:
        *   `auth_provider.dart`: وضعیت احراز هویت.
        *   `sentence_provider.dart`: مدیریت دریافت و ارائه جملات.
        *   `typing_provider.dart`: منطق مربوط به فرآیند تایپ و محاسبه نتایج.
    *   **`screens/`**: ویجت‌های مربوط به هر صفحه از اپلیکیشن:
        *   `auth_screen.dart`: صفحه ورود و ثبت‌نام.
        *   `home_screen.dart`: صفحه اصلی پس از ورود.
        *   `leaderboard_screen.dart`: جدول رده‌بندی.
        *   `profile_screen.dart`: پروفایل کاربر.
        *   `splash_screen.dart`: صفحه بارگذاری اولیه.
        *   `typing_screen.dart`: صفحه اصلی انجام تایپ.
    *   **`services/api_service.dart`**: مسئول ارتباط با API بک‌اند Django.
*   **`pubspec.yaml`**: فایل پیکربندی پروژه، شامل وابستگی‌ها (مانند `http`, `provider`, `shared_preferences`, `intl`) و تنظیمات Flutter.
*   **سایر پوشه‌ها (`android/`, `ios/` و ...):** کدهای مخصوص هر پلتفرم.

**ب) `typing_competition_project` (بک‌اند Django)**

*   **`manage.py`**: ابزار خط فرمان جنگو.
*   **`typing_competition_project/` (پوشه پروژه اصلی):**
    *   `settings.py`: تنظیمات اصلی پروژه جنگو (پایگاه داده SQLite، اپلیکیشن‌های نصب شده مانند `rest_framework`, `backend_api`, `corsheaders`, میان‌افزارها، و `CORS_ALLOW_ALL_ORIGINS = True` برای اجازه دسترسی از همه مبداها در حالت توسعه).
    *   `urls.py`: مسیریابی اصلی URL های پروژه.
*   **`backend_api/` (اپلیکیشن Django برای API):**
    *   `models.py`: تعریف مدل‌های پایگاه داده (احتمالاً `User`, `Sentence`, `TypingAttempt`).
    *   `views.py`: منطق پردازش درخواست‌های API (نوشته شده با Django REST framework).
    *   `serializers.py`: برای تبدیل مدل‌ها به JSON و بالعکس.
    *   `urls.py`: مسیریابی URL های مخصوص `backend_api`.
    *   `admin.py`: برای ثبت مدل‌ها در پنل ادمین جنگو.
    *   `migrations/`: تاریخچه تغییرات ساختار پایگاه داده.
*   **`db.sqlite3`**: فایل پایگاه داده پیش‌فرض.

### ۴. نکات و پیشنهادات

*   **ایجاد `requirements.txt` برای بک‌اند:** برای سهولت در نصب و مدیریت وابستگی‌های بک‌اند، اکیداً توصیه می‌شود یک فایل `requirements.txt` با دستور `pip freeze > requirements.txt` (پس از نصب تمام وابستگی‌ها در محیط مجازی) ایجاد شود.
*   **تنظیمات CORS در پروداکشن:** مقدار `CORS_ALLOW_ALL_ORIGINS = True` برای محیط توسعه مناسب است، اما در محیط پروداکشن باید به لیست دامنه‌های مجاز محدود شود.
*   **مستندسازی API:** برای بک‌اند، استفاده از ابزارهایی مانند Swagger یا OpenAPI برای مستندسازی خودکار API می‌تواند بسیار مفید باشد.
*   **تست‌نویسی:** اگرچه پوشه‌های `test` در هر دو بخش وجود دارد، تکمیل و گسترش تست‌ها برای اطمینان از پایداری و صحت عملکرد برنامه ضروری است.
*   **مدیریت خطا:** پیاده‌سازی مدیریت خطای مناسب در هر دو بخش فرانت‌اند و بک‌اند برای تجربه کاربری بهتر اهمیت دارد.
*   **امنیت:** بررسی موارد امنیتی، به خصوص در بخش بک‌اند (مانند اعتبارسنجی ورودی‌ها، جلوگیری از حملات رایج وب) باید در نظر گرفته شود.

این گزارش یک نمای کلی از پروژه بر اساس فایل‌ها و ساختار موجود ارائه می‌دهد. برای درک عمیق‌تر جزئیات پیاده‌سازی، نیاز به بررسی دقیق‌تر هر یک از فایل‌های کد می‌باشد.
